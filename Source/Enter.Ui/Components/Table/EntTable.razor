@namespace Enter.Ui.Components
@inherits Enter.Ui.Cores.Bases.EntComponentBase
@typeparam T
<CascadingValue Value="this">
    <table @attributes="AdditionalAttributes" class="@RootCss">
        @if (HeaderTemplate != null)
        {
            <thead>
            @HeaderTemplate
            </thead>
        }
        @if (DataSources != null)
        {
            <tbody>
            @foreach (var item in DataSources.Where(item => RowTemplate != null))
            {
                @RowTemplate?.Invoke(item)
            }
            </tbody>
        }


    </table>
</CascadingValue>